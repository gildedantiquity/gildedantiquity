# Internal editorial data

Isi folder ini **tidak untuk publik** dan **tidak ikut build Hugo / GitHub Pages**.

- `posting-schedule.json` — kalender perencanaan **3 artikel per hari** (editorial only).
- Dipakai oleh `scripts/generate_daily_posts.py` dan workflow `.github/workflows/daily-posts.yml`.

Jangan memindahkan file di sini ke `site/content/`, `site/data/`, atau `site/static/`.
